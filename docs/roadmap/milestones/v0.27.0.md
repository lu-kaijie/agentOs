# v0.27.0

本里程碑是 interactive model-backed agent shell 这条 change 的收尾版本。

完成内容：

- 常驻 shell、role、context policy、LangChain-native tools、session persistence 接成同一条工作流
- 真实模型路径可持续交互
- deterministic 路径仍保留

推荐体验：

```bash
make run-model
make shell-model
```
