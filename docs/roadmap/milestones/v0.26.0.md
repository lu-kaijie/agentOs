# v0.26.0

本里程碑完成了 model-backed agent runtime。

完成内容：

- `run --model` 与 shell 自然语言回合接入真实模型
- planner / executor / reviewer 走真实模型与真实工具
- 默认模型失败时允许清晰报错

推荐体验：

```bash
PYTHONPATH=src .venv-agentos/bin/python -m agentos.cli run \
  "请读取 README.md，并用两句话总结这个项目当前是什么。" \
  --model \
  --session-id real-model-demo
```
